name: Continuous integration

on:
  pull_request:

  workflow_call:

jobs:
  dotnet-format:
    uses: ./.github/workflows/dotnet_format.yaml
    with:
      ref: ${{ github.ref }}
      workspace: "./TestWebApi"
      dotnet-version: 6.0
